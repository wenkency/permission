// 整个项目的配置
ext {

    app = [
            compileSdkVersion: 30,
            minSdkVersion    : 14,
            targetSdkVersion : 30,
            versionCode      : 3,
            versionName      : "1.5.0"
    ]

    // 公共配置
    depsVersion = [
            appcompat          : "1.1.0",

    ]
    // 公共配置
    depsLibs = [
            appcompat       : "androidx.appcompat:appcompat:${depsVersion.appcompat}",

    ]
}