// 整个项目的配置
ext {

    app = [
            compileSdkVersion: 29,
            minSdkVersion    : 14,
            targetSdkVersion : 29,
            versionCode      : 1,
            versionName      : "1.0"
    ]

    // 公共配置
    depsVersion = [
            appcompat          : "1.1.0",

    ]
    // 公共配置
    depsLibs = [
            appcompat       : "androidx.appcompat:appcompat:${depsVersion.appcompat}",

    ]
}